# OCR Configuration
# GitHub Models Only - 2-Tier Strategy

# ⚠️ CHANGES:
# - Tesseract REMOVED (inconsistent accuracy)
# - PaddleOCR REMOVED (package build issues paddleocr@1.0.6)
# - Now using ONLY GitHub Models: GPT-4o (Tier 1) → GPT-4o-mini (Tier 2)

# Tier Strategy:
# Tier 1: GPT-4o (primary, best accuracy, ~15 req/min)
# Tier 2: GPT-4o-mini (fallback on rate limit, faster, higher limit)

# ===== GitHub Models Configuration =====
# Models are hardcoded in code:
# - gpt-4o (Tier 1)
# - gpt-4o-mini (Tier 2)
# No configuration needed - automatic fallback on rate limit
# - gpt-4o                          ✅ Best accuracy, 50 req/day
# - gpt-4o-mini                     ✅ Faster, potentially higher rate limit (RECOMMENDED)
# - Phi-4-multimodal                ⚠️ Microsoft's multimodal model (May not be available)
# 
# Note: Some models may not be available depending on your GitHub account

GITHUB_MODELS_MODEL=gpt-4o-mini

# GitHub Token (required for GitHub Models tier)
# GITHUB_TOKEN=your_token_here

# ===== PaddleOCR Configuration =====
# PaddleOCR Assets (only needed if using paddle tier)
# PADDLE_ASSETS_DIR=D:/auto-tool/ihit/puppeteer-automation/paddle-assets

